{
  "compilerOptions": {
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true
  },
  "tasks": {
    "dev": "deno run --watch --check --allow-net --allow-read --allow-env ./dev.ts",
    "build": "npm run clean && tsc",
    "lint": "deno lint"
  }
}